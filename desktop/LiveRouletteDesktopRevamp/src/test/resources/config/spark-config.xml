<?xml version="1.0" encoding="UTF-8"?>

<extentreports>
    <configuration>

        <!-- report theme -->
        <!-- STANDARD, DARK -->
        <theme>DARK</theme>

        <!-- document encoding -->
        <!-- defaults to UTF-8 -->
        <encoding>UTF-8</encoding>

        <!-- protocol for script and stylesheets -->
        <!-- defaults to https -->
        <!-- HTTP, HTTPS -->
        <protocol>HTTPS</protocol>

        <!-- offline report -->
        <timelineEnabled>true</timelineEnabled>

        <!-- offline report -->
        <enableOfflineMode>false</enableOfflineMode>

        <!-- use thumbnails for base64 images -->
        <!-- this may slow down viewing tests -->
        <thumbnailForBase64>false</thumbnailForBase64>

        <!-- TODO: Edit the title of the document -->
        <documentTitle>Live Roulette Desktop Revamp</documentTitle>

        <!-- report name - displayed at top-nav -->
        <reportName>Automation Test Report</reportName>

        <!-- timestamp format -->
        <timeStampFormat>MMM dd, yyyy HH:mm:ss</timeStampFormat>

        <!-- custom javascript -->
        <scripts>
            <![CDATA[
        $(document).ready(function() {

        });
      ]]>
        </scripts>

        <!-- custom styles -->
        <styles>
            <![CDATA[

      ]]>
        </styles>

        <!-- view order configuration -->
        <viewOrder>
            <view>dashboard</view>
            <view>test</view>
            <view>category</view>
            <view>exception</view>
            <view>author</view>
            <view>device</view>
            <view>log</view>
            <view>image</view>
        </viewOrder>

    </configuration>
</extentreports>